# Phase D — Inbound capture → intake (Delivery Hub, `delivery__`)
> Grounded read-only audit, 2026-06-18. Five real inbound channels terminate in `WorkItem__c` / `WorkItemComment__c`. A sixth (auto-diagnose agent) turns the captured field-change stream into a quoted, approval-ready `WorkRequest__c`. Core to the autonomy goal: agents watch inbounds → staged ready-to-approve work.

## 1. AS-BUILT FLOW (per channel → record)
**A. Slack (bidirectional).** Outbound (`DeliverySlackService`): stage changes, approval requests, connection-approval, comment mirror — all `@future(callout=true)`, gated on `SlackWebhookUrlTxt__c` + a feature flag (comment fan-out also needs `EnableSlackCommentSyncDateTime__c`). Inbound (`DeliverySlackInboundHandler` impl `IWebhookEventHandler`): Slack Event API → `DeliveryWebhookReceiverApi` (`@RestResource /deliveryhub/v1/webhook/*`) → `DeliveryWebhookEventRouter.findInbound('Slack_Webhook')` resolves `IntegrationProvider__mdt` → `WebhookSignatureVerifier.verify` (HmacSha256Slack) → regex `T-NNNN` in `event.text` → resolve WI by Name → insert `WorkItemComment__c` (`AuthorTxt__c='Slack: <id>'`, `SourcePk__c='API'`). Echo-suppression: bot messages dropped; outbound skips `Slack`-authored comments. URL-verification `challenge` echoed by receiver. **Produces a comment on an existing WI — not a new WI.**
**B. Inbound email (email→comment).** `DeliveryInboundEmailHandler` (`Messaging.InboundEmailHandler`): resolve sender → active User (reject if not); gate sender's `EnableEmailNotificationsDateTime__c`; parse `T-NNNN` from to/subject (normalizes `T-39`→`T-0039`); strip reply-quote; insert `WorkItemComment__c` (`SourcePk__c='Email'`); notify subscribers. Reply-to `workitem-T0039@{domain}`. **Comment on existing WI.**
**C. Cross-org DH sync (inbound).** `DeliveryHubSyncService.handleIncomingSync` gates (key→rate→HMAC→echo→Pending-Approval→DepthProbe) → `DeliverySyncItemIngestor.processInboundItem`. Pull: `/sync/changes?since=&clientId=`. Ingestor (~1289 lines): ledger resolution, cross-org FK translation, child-before-parent Pending queue, auto-parent to sender Client entity, **rejects blank WI creates**, predecisional gate, NetworkEntity→Pending Approval, savepoint atomicity, WorkLog content-dedup. **Produces full records:** WorkItem/Comment/WorkLog/Dependency/ContentVersion/NetworkEntity.
**D. Public web submission (unauth).** `DeliveryPublicSubmissionService.submitWorkItem` (`/deliveryhub/v1/submit`, guest): validate title+email, rate-limit 5/hr/email, sanitize HTML, resolve/fallback NetworkEntity. **Creates `WorkItem__c`** (`[Public] title`, Backlog/New, `RequestTypePk__c='Public Lead'`).
**E. Authenticated portal/API.** `DeliveryPublicApiService` (`/deliveryhub/v1/api/*`): `POST work-items` → `DeliveryPortalController.submitPortalRequest` (WI Backlog/New); `POST work-items/{id}/comments`; `log-hours`; `approve/reject-worklogs`. Auth = `X-Api-Key` → Connected NE; optional `X-Portal-User`.
**Live push (overlay):** `DeliveryWorkItemChange__e` published on stage change, gantt-field change (gated), and comment insert/update → LWC subscribers re-render sub-second. **A Slack/email/sync-created comment fires the event → UI live-refreshes without polling.**

## 2. USER STEPS
**Slack:** admin creates Slack app (Incoming Webhook + Event Subscriptions → Request URL `<site>/.../webhook/Slack_Webhook`, subscribe `message.channels`), invites bot, pastes signing secret into `IntegrationProvider.Slack_Webhook.SignatureSecretTxt__c` + stamps `EnabledDateTime__c`, sets `SlackWebhookUrlTxt__c` + `EnableSlackCommentSyncDateTime__c`. End-user types `T-0123 …` → comment appears. **Email:** admin creates Email Service → `DeliveryInboundEmailHandler` + an address; per-user `EnableEmailNotificationsDateTime__c`; user replies to `workitem-T0039@…`. **Sync:** admin connects/approves NetworkEntity; flows automatically. **Public:** user fills web form → `POST /submit` → WI in Backlog/New.

## 3. GENERALIZABLE vs MF-SPECIFIC
Generalizable: all channels + the webhook substrate (router + `IntegrationProvider__mdt` + `IWebhookEventHandler` — add a provider = 1 CMT row + 1 class; Stripe already ships as a 2nd provider); `DeliveryAutoDiagnoseService` + `DiagnoseRule__mdt` are explicitly generic; `T-NNNN` is the package AutoNumber. MF-specific (config): the daisy-chain topology; `Slack_Webhook`/`Stripe_Webhook` CMT ship with `EnabledDateTime__c`/secret nil (per-org activation); no Email Service ships.

## 4. GAPS / CAVEATS (honest)
1. **🟠 Slack fully built + bidirectional but ships OFF** — provider CMT `EnabledDateTime__c`/secret nil; until set, receiver answers 404. Code-complete, config-gated.
2. **🔴 Inbound email has NO Email Service in metadata** (`**/*.email-meta.xml` = zero files) — handler exists + tested, but the Email Service + address must be created manually in Setup per org. **Cannot work on install without admin setup.** Sender must also be an active SF User with the per-user flag.
3. **🔴 Slack inbound + email only mint comments, never new WIs** — both attach to an existing `T-NNNN`. New-work intake exists only via public submission (D), portal API (E), cross-org sync (C), and the auto-diagnose agent.
4. **🟠 Slack inbound `SourcePk='API'`** (picklist lacks Slack) — provenance only in author/body. Author is an unresolved Slack user id (no SF User mapping).
5. **🟠 Auto-diagnose (agent intake) ships OFF** behind two gates + **no DiagnoseRule CMT ships** — no-ops until an admin authors a rule AND the SEE Flow feeds `ActivityLog__c`. Built, flip-ready, not running.
6. **🟠 Public submission auto-resolves to "first active Client"** when no entityId — multi-tenant mis-attribution risk.
7. **🟠 Cross-org key/HMAC opt-in** (no key = open access). **🟠 Echo-suppression is 300s-bounded** — a >5-min stalled round-trip could re-process.

## 5. PROS / CONS + ENHANCEMENTS (unscoped)
Pros: one pluggable webhook substrate serves all HTTP inbound; strong ingestor idempotency (ledger + GlobalSourceId + content-match); blank/predecisional guards; live platform-event push; feature-flag discipline. Cons: the two most-demoable channels (Slack inbound, email) need manual per-org setup nothing automates; Slack/email can't create intake; provenance leaks; auto-diagnose needs a Flow + rule that don't ship; fragile public-lead attribution.
Enhancements: ship a packaged Email Service/address (or provision via install handler); add `Slack` to `SourcePk__c` GVS + map Slack→SF User; Slack slash-command/shortcut to *create* a WI; ship a starter (off) DiagnoseRule + the SEE Flow template; email-to-WorkItem when no `T-NNNN` matches (gated); inbound-channel dashboard (counts by source/failures/last-received); default-require key/HMAC for new connections.

## 6. COWORK TEST SCENARIOS (exhaustive — 84; Pre→Action→Expected→Verify). `delivery__` prefix on subscriber orgs.
**A. Slack inbound (1–17):** happy T-NNNN comment; lowercase/embedded match; no-match ignored; WI-not-in-org ignored; empty text ignored; bot-echo guard (no dup); author-prefix loop guard (no re-post); url_verification challenge echo; bad signature 401; provider disabled 404; unknown slug 404; missing slug 400; unsupported envelope; event_callback missing event; non-message inner type; guest-permission gap (HTML not JSON); malformed JSON 500.
**A2. Slack outbound (18–22):** stage-change post; comment fan-out; outbound-OFF no-op; testWebhook success + bad-URL.
**B. Inbound email (23–33):** happy reply→comment; subject-only T-NNNN; reply-quote stripping; HTML-only body; empty body "(empty)"; non-user sender rejected; not-opted-in disabled; no T-NNNN; T-NNNN not in org; number normalization; **no Email Service configured (the out-of-box gap)**.
**C. Cross-org sync inbound (34–59):** happy WI push + ledger; blank-WI rejected 500; idempotent re-delivery (UPDATE); cross-path dedup (GlobalSourceId); WorkLog child→Pending; parent arrives → resolver replays; comment child→Pending; dependency dual-FK Pending; WorkLog content-dedup (no twin); NetworkEntity→Pending Approval + Slack; sender pending→403; echo suppress (recent); echo NOT suppress (>300s); bad key 401; no key (open); HMAC mismatch 401; rate limit 429; predecisional gate blocks; predecisional opt-in passes; empty payload 400; invalid object type 500; stale TargetId → insert (heal); pull /changes flips Synced; pull missing clientId 400; /sync/health; ContentVersion pull-callout file → fetcher enqueued.
**D. Public submission (60–67):** happy 201; missing title 400; bad email 400; rate-limit 5/hr 429; HTML sanitization; entityId valid → linked; entityId omitted → first-active-Client (mis-attrib risk); title truncation.
**E. Portal/API (68–75):** POST work-items 201; blank-payload 400; missing key 401; invalid key 401; POST comment 201; comment-on-inaccessible-WI 403; API rate limit 429; X-Portal-User delegation.
**F. Agent intake (76–80):** both gates off no-op; no active rule no-op; threshold crossed → drafts WR into queue (marker title); cooldown dedup; below threshold nothing.
**G. Cross-channel/live (81–84):** Slack inbound fires `comment_insert` → chat LWC refreshes; email inbound live-refresh; sync-created comment live-refresh; live-gantt events gated.

## 7. AGENT OPERATING INSTRUCTIONS (instruction-engine)
Goal: watch inbound signals → convert to staged, ready-to-approve `WorkRequest__c` (Offer-Sent), route ambiguity to a human. Grounded in `DeliveryAutoDiagnoseService` → `DeliveryWorkApprovalService.submitForApproval`.
**Reads:** `ActivityLog__c` Field_Change (the SEE stream); new `StatusPk__c='New'` intake WIs from public/portal/sync; `WorkItemComment__c` `SourcePk__c IN ('Email','API')`; settings flags + active `DiagnoseRule__mdt`. **Decides:** actionable anomaly/request? estimate quoted hours; below `DiscretionaryThresholdHoursNumber__c` → auto-accept allowed, above → Offer-Sent for human; dedup (cooldown marker / already-priced). **Writes:** a `WorkItem__c` + `submitForApproval(ids, hoursMap)` → quoted WR in queue. **Never** writes an Approved state directly. **Routes to human:** quote ≥ threshold; ambiguous Slack/email comments naming no `T-NNNN`; public leads with no resolvable entity; predecisional/closed-gate records (403). **Errors:** idempotency first (marker cooldown + once-per-day guard); blank-content guard; bounded sweeps (`MAX_LOG_ROWS=5000`); respect `EnableAutoDiagnoseDateTime__c`, never self-enable. **Automatable today:** anomaly→quoted WR (flip-ready once flag+rule+SEE Flow exist), dedup/cooldown, queue submission, discretionary auto-accept. **Needs build/human:** no DiagnoseRule/SEE-Flow ship (admin authors both); Slack/email→new-WI intake not built; sizing of unpriced intake WIs not implemented (only static rule hours); approval above threshold stays human.

Source: phase-D research agent (add05e52319044845) — full per-scenario detail available.
