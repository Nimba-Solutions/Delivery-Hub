<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RequiresWalkthroughDateTime__c</fullName>
    <description>Populated = a live walkthrough is required before this track can be marked complete; null = the track is fully self-serve. The timestamp itself records when the walkthrough requirement was activated on the catalog entry. Pattern per docs/FIELD_NAMING.md (no Booleans — DateTime stamps).</description>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <label>Requires Walkthrough</label>
    <required>false</required>
    <type>DateTime</type>
</CustomField>
